## Code Review Task

Review all changes in this branch compared to `main`. This is a Rust project (actionbook-rs).

Focus on:
1. Logic correctness — especially wss vs local CDP path branching, connection handling, error recovery
2. Race conditions — WebSocket connection lifecycle, session management
3. Error handling completeness — all failure paths should be handled gracefully
4. Type safety — Rust-specific: unwrap() usage, Option/Result handling
5. Security — no credential leaks, proper URL validation
6. Backwards compatibility — local port mode (`--cdp 9222`) must still work

List any P1 (must fix) or P2 (should fix) issues found. If none, state "No P1/P2 issues found."

Run: `git diff main` to see the changes.