---
"@actionbookdev/cli": minor
---

Add `browser stop` to release an Actionbook-owned local Chrome session while retaining its named profile and authentication state. `browser close` remains the explicit destructive command for deleting non-default local profiles.
