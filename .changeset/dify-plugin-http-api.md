---
"@actionbookdev/dify-plugin": patch
---

Replace hyperbrowser SDK with direct HTTP API, add HTML misroute guard, lazy-load browser deps for serverless compatibility, and fix session recovery credentials
